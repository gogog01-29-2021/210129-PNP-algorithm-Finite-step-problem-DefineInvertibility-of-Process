def solution(before, after):
    a=sorted(list(before))
    b=sorted(list(after))
    print(a,b)
    return int(a==b)

before,after="string"
list()    [, , ,]
.split()   =>gives  .split(" ")
